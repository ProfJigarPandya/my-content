Assume that an application runs on a single computer and you want to convert it to
run in a “distributed” manner on a network. Write a program to demonstrate the
stepwise conversion of this program that writes a message to the system console.
Use rpc.

RPC

Courtesy: http://digital.cs.usu.edu/~allan/DNP/Slides/RPC.pdf

Create date.x file with appropriate content

rpcgen -C -N -a date.x to generate templates

Edit server and client file as per requirment
