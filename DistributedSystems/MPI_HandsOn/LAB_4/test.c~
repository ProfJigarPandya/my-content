#include <stdio.h>

int main()
{


int input[][]={{1, 5000},{5001,10000},{10001,15000},{15001,20000}};

int total=0;
int start;
for(i=0;i<4;i++)
{
	for(start=input[i][0];start<input[i][1];start++)
	{
		total+=start;
	}
}
printf("\n %d",total);
return(0);
}
